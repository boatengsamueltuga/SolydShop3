spring.application.name=sb-ecom

# configuration for H2 database
# spring.h2.console.enabled=true
# spring.datasource.url=jdbc:h2:mem:test
# spring.jpa.show-sql=true

## Configuration for MySQL
# spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce
# spring.datasource.username=root
# spring.datasource.password=boa@450$asdF
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# PostgreSQL configuration (env vars with local defaults)
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:ecommerce}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:boa@450$asdF}
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.ddl-auto=create
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=${SHOW_SQL:true}

# Image upload configuration
project.image=images/
image.base.url=${IMAGE_BASE_URL:http://localhost:8080/images}

# JWT CONFIGURATION
app.jwt.secret=${JWT_SECRET:06cnbi09sdA8wZXANy9ktApDHJAC1Oz0Dr0Fn+TeqU4=}
app.jwt.expiration-ms=${JWT_EXPIRATION:3600000}
spring.ecom.app.jwtCookieName=springBootEcom
app.cookie.secure=${COOKIE_SECURE:false}

# Logging (optional)
# logging.level.org.springframework=DEBUG
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.springframework.security=DEBUG
# logging.level.com.ecommerce.sb_ecom=DEBUG

# Frontend
frontend.url=${FRONTEND_URL:http://localhost:3000/}

# Server port (Render sets PORT env var)
server.port=${PORT:8080}

# STRIPE KEY
stripe.secret.key=${STRIPE_SECRET_KEY}

# Cloudinary (image hosting)
cloudinary.url=${CLOUDINARY_URL:}
